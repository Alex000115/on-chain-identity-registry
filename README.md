# on-chain-identity-registry
A professional, Soulbound Token (SBT) inspired identity registry that allows users to link their wallet addresses to verified metadata (like a username or profile hash) which cannot be transferred once issued.
